<h1>Wikinimous</h1>

<%= link_to "New article", new_article_path %>

<ul>
  <% @articles.each do |article| %>
    <li>
      <h2><%= link_to article.title, article_path(article) %></h2>
      <p><%= truncate article.content, length: 60 %></p>
    </li>
  <% end %>
</ul>
